# Core numerical and visualization (Phase 1: M1–M3)
numpy>=1.24.0
matplotlib>=3.7.0
scikit-learn>=1.3.0  # Data generation only (e.g., make_moons in NN Deep Dive Lab)

# Jupyter for notebooks
jupyter>=1.0.0
ipykernel>=6.0.0

# Deep learning (Phase 1: M4, Phase 2–4)
torch>=2.0.0
